spec:
  entryPoints: [websecure]
  routes:
    - match: Host(`ai.mvgates.com`)
      kind: Rule
      middlewares:
        - name: webui-streaming
      services:
        - name: open-webui
          port: 8080
          serversTransport: webui-transport
