apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openclaw-state-pvc
  namespace: management
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 5Gi
